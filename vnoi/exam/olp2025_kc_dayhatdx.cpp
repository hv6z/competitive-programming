#include <bits/stdc++.h>

using namespace std;

bool doixung (string s){
	
	return true;	
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int n;
    cin >> n;
    string a[n];
    for(int i = 0; i < n, i++){
    	cin >> a[i];
	}
	
	cout << doixung("abcba");
    return 0;
}